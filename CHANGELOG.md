# Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 的基本格式。正式发布后在这里
记录用户可见变化；尚未发布的修改放在 `Unreleased`。

## 1.1.0 - 2026-07-28

### Added

- 新增未加密、可重排 MOBI 的服务端导入，支持 MOBI7/KF8 章节、目录、书名、作者和内嵌封面，并接入
  网页阅读、Android 离线下载与进度同步；DRM 和 Print Replica 文件会明确拒绝。
- 新增 `/reader` 网页阅读器，支持书架切换与搜索、受保护书架解锁、TXT/EPUB/MOBI 连续纵向阅读和自动续章、
  PDF 原版页面与目录导航、阅读进度同步以及本地阅读偏好。

### Changed

- Android 离线朗读改用 sherpa-onnx、`matcha-icefall-zh-en` 与 Vocos，支持中英文混合文本，全程端侧
  合成且无需额外服务。
- 移除 Piper 双音色、g2pW 模型及其词典生成链路；Matcha 当前只提供一个女声音色。

### Fixed

- Android 在远端不可用或会话过期时，只要存在文件完整的永久下载，仍可启动并进入离线书架。

## 1.2.0 - 2026-08-07

### Added

- Android 朗读新增睡眠定时：可设置 15/30/45/60 分钟或播放完当前章后自动停止。
- 管理后台支持将单本书从书架中隐藏或恢复（读者端不再显示，管理端仍可见并可一键恢复）。
- 书架扫描间隔支持按分钟、小时、天、周设置。
- 章节拆分新增独立的“古文拆分”方式：识别卷体（卷X/卷之X/卷上中下/上中下卷）、第X回/则/出/品、
  经书篇名+序数（如“学而第一”）、史书纪传表志（如“项羽本纪”）与组诗“其一/其二”等古籍体例。

### Changed

- 移除登录会话的有效时限：内网部署下 App 与管理后台会话不再定时过期，仅在服务器修改管理密码后
  全部失效（`ADMIN_SESSION_HOURS` 配置项已移除）。
- 移除“隐藏书架”功能，书架只保留可选的四位访问密码。
- 新建书架不再显示“相对目录”输入，书架直接使用所选存储位置的整个目录。
- 点击“创建并扫描”后创建卡片立即关闭，首次扫描在后台继续。

### Fixed

- 升级自旧版数据库后自动移除遗留的 `is_hidden` 非空列，修复新建书架被误报为“名称或目录已存在”。

## 1.0.9 - 2026-07-25

### Fixed

- 修复 Docker 只读挂载的书库被错误要求写权限，导致后台无法登记存储位置的问题。

## 1.0.8 - 2026-07-24

### Added

- AGPL-3.0-only 项目许可证与完整第三方许可证清单。
- GitHub Issue/PR 模板、Dependabot、CI、Git LFS 和贡献文档。
- 首次登录强制修改默认管理密码，并提供可持久化的哈希凭据和后台改密入口。
- GitHub Release 自动发布 Android APK、Docker 部署包和 SHA-256 校验文件。
- GHCR 提供 `linux/amd64` 与 `linux/arm64` 后端镜像。
- README 展示管理界面，并明确项目不包含或分发任何图书资源。
- TXT/EPUB/PDF 阅读、离线下载和跨设备阅读进度同步。
- 网页管理后台、书架扫描、单书章节策略和封面管理。
- sherpa-onnx、Piper 与 g2pW 完全离线中文朗读。

### Changed

- 公开仓库使用通用 Docker 挂载和 Android 默认服务器地址，不包含开发者本地路径或 IP。
- Docker Compose 项目和容器统一命名为 `page-shelf`，README 补充管理地址、默认凭据、目录与 App 连接信息。
